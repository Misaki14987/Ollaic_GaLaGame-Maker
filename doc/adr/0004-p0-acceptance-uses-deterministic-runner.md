# P0 Acceptance Uses a Deterministic Runner

P0 validates the Agent Flow runtime with a deterministic runner while allowing Plan and Outline Steps to use a real AI provider when one is configured. This keeps the foundation testable without network or model variance, while still letting the V2 FlowBoard demonstrate real generation in configured environments.
