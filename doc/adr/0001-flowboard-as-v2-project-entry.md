# FlowBoard as the V2 Project Entry

V2 uses FlowBoard as the project-first workspace while preserving the existing scene, asset, and character editors as deeper editing surfaces. This chooses a flow-first product shape without making StoryPlan IR the only authoring surface, avoiding the risk of replacing the current WebGAL editing model before the generation pipeline has proven itself.
