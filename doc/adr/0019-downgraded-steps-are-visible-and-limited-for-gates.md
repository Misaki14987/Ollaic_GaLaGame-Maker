# Downgraded Steps Are Visible and Limited for Gates

When the user accepts a capability downgrade, ordinary Flow Steps may still run and succeed with a visible downgrade warning. Critical Quality Gates cannot be treated as fully passed when they rely on downgraded capabilities, because the user needs to distinguish "the flow completed" from "the output is trusted enough to preview or export."
