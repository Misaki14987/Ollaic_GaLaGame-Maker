# User Editable Flows Can Be Saved as Templates

V2 lets users save edited Agent Flow graphs as reusable Flow Templates instead of limiting dependency edits to a single run. This makes FlowBoard a reusable generation design surface across Projects, at the cost of adding template versioning, compatibility checks, migration rules, and clearer separation between a Flow Template and the StoryPlan produced by a specific run.
