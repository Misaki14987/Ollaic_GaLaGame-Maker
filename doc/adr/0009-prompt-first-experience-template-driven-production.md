# Prompt-First Experience, Template-Driven Production

V2 starts from a Production Brief in the user's language, but stabilizes execution by mapping that brief onto a Flow Template. This keeps the first interaction natural and low-friction while making generation repeatable, inspectable, and reusable instead of inventing a fresh workflow for every project.
