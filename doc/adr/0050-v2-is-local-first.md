# V2 Is Local-First

V2 keeps Projects, playable files, assets, Flow history, AI settings, Creator Preferences, and Flow Artifacts local-first instead of making the cloud the system of record. Cloud features may later support optional sync, backup, sharing, or template distribution, but they must not replace the user's local Project as the primary workspace.
