# Release Candidate Checks Use Release Scope

Release Candidate checks use a Release Scope instead of assuming every visual novel must validate the same set of endings or branches. Flow Templates can recommend a scope, and users can choose one primary ending, all endings, or a selected route set so Quality Gates and Asset Coverage match the production target.
