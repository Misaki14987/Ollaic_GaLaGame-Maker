# Single Language Is a Parameter, Localization Is a Module

V2 treats the primary creation language as a Template Parameter, while multi-language delivery is handled by a Localization Module. This keeps ordinary single-language production simple and gives localization its own Steps for translation, terminology consistency, and language-specific review when users need multi-language output.
