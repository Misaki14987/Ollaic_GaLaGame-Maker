# Agent Flows Are Project-Scoped

The core Agent Flow model is scoped to one Project, even if future batch production coordinates multiple Project Flows. This preserves the local Project as the playable content boundary and avoids giving a single Flow multiple competing project file systems, histories, and quality states.
