# Content Rating Affects Template Match with Override

Template Match uses Content Rating to prefer compatible Flow Templates and Flow Modules instead of treating sensitive content as only a passive warning. Users may override the recommendation, but overridden matches must show the compatibility and provider risk clearly before the Agent Flow runs.
